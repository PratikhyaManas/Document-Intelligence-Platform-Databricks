# Local/dev dependencies for editing & unit-testing the notebooks & src/
# helpers outside the Databricks workspace (a real cluster provides
# pyspark, databricks-vectorsearch, mlflow, etc. natively).

databricks-connect>=15.4
databricks-sdk>=0.30
databricks-vectorsearch>=0.40
databricks-agents>=0.10
mlflow>=2.16
pyspark>=3.5
pandas>=2.2
pytest>=8.0
